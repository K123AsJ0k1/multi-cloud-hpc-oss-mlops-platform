apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: ray-local-2-virtualservice
spec:
  hosts:
  - "ray.local-2.oss"
  gateways:
  - dashboard-gateway
  http:
  - route:
    - destination:
        host: ray-local-dash-2.integration.svc.cluster.local
        port:
          number: 8381